Drag {
    starting_x: isize,
    starting_y: isize,
    ending_x: isize,
    ending_y: isize
}


CustomInput {
    And(CustomInput, CustomInput),
    Or(CustomInput, CustomInput),
    Not(CustomInput),
    Keyboard(KeyCode),
    MouseClick(MouseClick),
    MouseMovement(MouseMovementDirection),
    MouseWheel(MouseWheelDirection),
    MouseDrag(MouseDrag),
    GamepadButton(GamepadButton),
    GamepadStick(GamepadStickDirection),
    GamepadConnection(GamepadConnection)
}

(Ctrl + Shft + (W | Up)) | (MouseUp + !MouseClick)

Or(And(And(Ctrl, Shift), Or(W, Up))), And(MouseUp, Not(MouseClick)))


Or(
    And(
        And(
            And(
                Or(
                    Keyboard(LCtrl),
                    Keyboard(RCtrl)
                ),
                Or(
                    Keyboard(LShift),
                    Keyboard(RShift)
                )
            ),
            Or(
                Keyboard(W),
                Keyboard(Key1)
            )
        ),
        MouseClick(Right)
    ),
    Not(
        MouseClick(Left)
    )
)